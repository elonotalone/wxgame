{
  "name": "wxgame-web-preview",
  "version": "0.1.0",
  "private": true,
  "description": "Browser preview for /src/ WeChat mini game (vibe coding dev only)",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3007 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3007"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
